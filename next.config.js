// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // No experimental block needed for basic app
  // Only add serverExternalPackages if using Firebase Admin in Server Actions
}

export default nextConfig;